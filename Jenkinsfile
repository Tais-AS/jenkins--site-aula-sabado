pipeline {
    agent any

    stages {
        stage('Build') {
            steps{
                sh 'echo"Sábado de aulas intesivas"'
                sh '''
                    echo "Nosso Sábado"
                    ls-la
                '''
            }
        }
    }
}
